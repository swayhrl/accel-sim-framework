# Tiny BFS smoke input

`graph4096.txt` is a synthetic tiny BFS smoke input retained under the historical filename solely for compatibility with existing BFS arguments. It is not the official 4096-node dataset.

`bfs-rodinia-2.0-ft/data` is a relative compatibility symlink required by the launcher, which resolves each suite data root beneath the benchmark name.
