# Offline-Sim V1 agent rules

Work only inside this repository. Framework and nested `gpu-simulator/gpgpu-sim` are independent Git repositories. Do not alter simulator behavior for offline packaging. Offline commands must not implicitly use network; cache all sources/assets first and record SHA256. Commit Framework work only to `task/offline-sim/bootstrap-v1`.
