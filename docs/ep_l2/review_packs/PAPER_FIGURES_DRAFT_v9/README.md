# 合并距离桶的时序复用距离图

距离桶按不重叠定义合并为：`≤256`（保留原 `≤8` 的颜色）、`257–512`（保留原 `257–512` 颜色）、`513–2048`（使用原 `1025–2048` 颜色）、`>2048`（使用原 `>4096` 颜色）。

“512–4096”与“>2048”原本重叠，因此按可重现且不重叠的阈值解释为 `513–2048` 与 `>2048`。所有数值直接由 v8 的冻结精确距离桶 CSV 相加得到，未改动科学数据。
