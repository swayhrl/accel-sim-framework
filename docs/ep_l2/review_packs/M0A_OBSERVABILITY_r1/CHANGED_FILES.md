# Changed files

Core: `gpu-cache.h`, `gpu-sim.cc`, `l2cache.h`, `l2cache.cc`. Framework: M0a configs, parser, runner, analyzer, unit tests, retained-log finalizer. Source audit shows the enable bit gates collection/emission only, not admission, allocation, replacement, arbitration, lower routing, scheduler, or DRAM behavior.
