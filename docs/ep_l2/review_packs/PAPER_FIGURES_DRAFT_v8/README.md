# 非零阻塞 roster 的时序扇区复用距离图

本图与 v7 使用相同的 workload 集合：`dwt2d`、`convolutionSeparable`、`spmv`、`scan`、`FWT_7_21`、`cfd_097k`、`btree`。柱形仍仅条件化于真实时序扇区复用，纵轴为时序复用事件中的距离分布百分比；每个柱顶 `T=` 是所有扇区引用中的真实时序复用比例。

输入仅为 v2 冻结绘图 CSV；未重跑模拟器、未修改科学数值或既有图件。
