# Pre-final opportunity interpretation

RO / pending state: `UNCERTIFIED_CANDIDATE_ONLY`.  The 9-of-10 data quantify
an epoch-safe candidate population and exact pre-ready milestones, but do not
prove a safe RO request class.  A focused source certification contract is a
prerequisite to any functional M3 design.

TVD: the four completed workloads with dirty victims report 683,333 valid old
resident handles after replacement/reassignment, and 0 remain live.  Thus the
narrow current-model result is
`NO_OLD_RESIDENT_PAYLOAD_HANDLE_HOLD_TO_SET_DONE_OBSERVED`.  The modeled WAD
still releases at `set_done`, but the old resident payload identity is not
what it holds.  This narrows the particular early-resident-payload-release
motivation; it is not a claim about every possible decoupling mechanism.

Shared payload: completed production demand is 1,842,117 resident allocations
and 0 non-resident allocations.  The correct pre-final label is
`NO_REAL_NONRESIDENT_CONSUMER_OBSERVED_9OF10`; dormant bypass capacity and
synthetic traffic are not evidence.

Final breadth remains pending the existing scan row.  No functional feature,
policy, RO transition, TVD release, or shared-pool allocation was implemented.
