# Figure 2 composition — reference-style draft

New figure-only visual treatment. It uses the same frozen five-class blocked-cycle composition table as v4, but adopts the requested compact 100%-stacked-bar visual style.

For workloads with zero total blocker cycles, the full-height light-gray hatched column is explicitly `No blocking`/N.A.; it is not a sixth blocker class and does not fabricate a five-class composition. All nonzero bars are exactly 100% among `SET_ASSOC`, `MSHR_META`, `MISSQ_LOWER`, `WB_PATH`, and `OTHER`.
