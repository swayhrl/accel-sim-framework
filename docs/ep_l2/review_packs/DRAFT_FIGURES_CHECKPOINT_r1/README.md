# Streaming-Reuse draft plotting checkpoint r1

Purpose: visual review only. This directory is a new draft artifact and does not modify `STREAMING_REUSE_PREFINAL_r1/`, `STREAMING_REUSE_CHARACTERIZATION_r1/`, `MOTIVATION_FIGURES_r1/`, or any result root.

Included formal rows (all `COMPLETE_VALID`): `scan`, `vectorAdd_4M`, `convolutionSeparable`, `spmv`, `FWT_7_21`, `cfd_097k`, `dwt2d`, `sad`, `btree`, `gemm`, `BlackScholes`, `mergeSort`, and `transpose`. `scan` completed naturally and is included; no formal slot is pending in this checkpoint.

`figures/` contains PNG and SVG drafts. `plotting_tables/` contains the exact CSV inputs used for each panel. Figure 1 v2 Panel A is normalized over all sector-reference events. Panel B is conditional on true temporal-sector reuse and records `NO TEMPORAL REUSE`/`N/A` for zero-temporal rows. Figure 2 uses the WBUF=8 machine-readable structural-breakdown CSV: segments are fractions of eligible miss-admission cycles and each bar label is the overall projected blocking rate.

These figures are not a scientific final PASS or a final review pack.

