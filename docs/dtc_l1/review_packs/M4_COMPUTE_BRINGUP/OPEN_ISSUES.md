# Open issues

None for authorized M4 HARD gates.

F01--F03 remain `SOURCE_UNREACHABLE_NA` for frozen source, not implemented or waived as PASS. Future source that produces a real `FENCE_OP` must reactivate fence validation.

PB_2DCONV Base remains `SLOW_BUT_PROGRESSING` after 420 seconds and is only a recovery diagnostic. M5 performance work is not authorized.

An initial temporary mixed-program build statically linked CUDA runtime and never entered GPGPU-Sim; it is excluded. The accepted shared-runtime `.cg` run is indexed below.
