# Open issues and boundary

There is no open scientific action within this pre-registered stage.  The four initial C1 directories listed in `RAW_LOG_INDEX.tsv` stopped after initialization without a terminal receipt; local cgroup evidence recorded no new OOM kill and the persistent-supervisor fresh attempts all natural-exited and strict-PASSed.  The incomplete directories remain immutable non-scientific attempts and are not deleted or used.

The remaining limitation is intentionally preserved: this is one source-defined `busW` upper-bound counterfactual, not a broad DRAM or interconnect sensitivity study.  A different service resource, new architecture mechanism, or a broader workload claim would cross the stated claim boundary and requires review before any new simulation.
