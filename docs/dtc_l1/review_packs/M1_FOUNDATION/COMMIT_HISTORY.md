# Semantic commits

Core range `cfbd365e..48b0be73`:

- `add385ca` directed common front-end tests;
- `007b2571` Paper Base PIB and Tag gate;
- `06a2e689`, `e363a773` accounting and primary stalls;
- `d5325305` conventional MSHR override and lower cap;
- `581fff76` deterministic LEGACY micros;
- `06aa534a` B07 true-hit PIB retirement recovery;
- `48b0be73` independent baseline resource counters.

Framework range `9c2e10a..ff26ef46` records the B07 evidence and parses the
new counters. Every commit was explicitly path-staged and pushed.
