# AWMA first current-model baseline simulation report — 174-new

`SIM_COMPAT_CAPTURE_V1_QUALIFIED`

`FIRST_CURRENT_MODEL_BASELINE_SIM_PASS`

The exact Q05 durable bundle was independently admitted through the LDGDEPBAR-aware strict validator. The stable input ID is `SIM_INPUT_0ab4e2fe2195d3b7d7da4ee9df6017cbec5c063fcc8110374e1a753f87177634`.

Using the recovered authority F0 Prefill config, qualified binary, restricted input-specific overlay, and 10,000-cycle policy, two bounded replays ended at the expected maximum-cycle boundary. Both telemetry exports passed. Determinism is PASS after normalizing only run-local `run_dir` provenance; raw logs and raw telemetry remain preserved in the review pack.

No full-ROI claim, mechanism sweep, or Native numerical calibration was performed.