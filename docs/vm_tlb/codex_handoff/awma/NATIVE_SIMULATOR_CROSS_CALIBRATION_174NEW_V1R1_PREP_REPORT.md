# Native ↔ Simulator Cross-Calibration 174-new V1R1 Prep Report

The existing M0–M3 payloads are conclusively the two-sample warmup `chase_occurrence_0`, not the 50-sample measured occurrence1. No accepted occurrence1 payload or full capture CLI survives in the node164 bundle, and the permutation seed remains unresolved.

Independent opt-in telemetry observed every active dependent load after coalescing. M0/M1/M3 each contain 1,024 events and M2 contains 16,384; all have one active lane and `accessq_entries=1`. M1/M2 C0, C1 diagnostics-off, and C1 diagnostics-on scientific outputs are exactly equal. The suite is therefore classified `M0_M3_MECHANISM_INACTIVE_CONTROL_SUITE`.

All 38 distinct SM89 trace opcode spellings resolve through the existing Ampere map. The essential `LDG.E` address/mask/global-load path and `CS2R` issue-identity use are closed for trace replay; `LDG.E.STRONG.GPU` has zero active mask throughout. This is parser admission only, not an Ada-fidelity claim.

Cross-Cal V1's 24-point outputs remain valid as `CROSSCAL_V1_WARMUP_TRACE_SUPPORTING_ONLY`. Exact Native M1/M2 disagreement and V1/V2R1 Native-alignment classifications remain unadmitted pending measured occurrence1 traces with closed seed and contextual replay. No matrix/AI rerun, semantic change, tuning, baseline promotion, or new mechanism was performed.
