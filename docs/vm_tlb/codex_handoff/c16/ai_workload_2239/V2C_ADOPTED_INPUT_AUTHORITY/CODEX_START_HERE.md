# Codex Start Here — V2C Adopted Input Authority

Branch:

```text
hrl/c16-ai-workload-adopted-input-v2c
```

Read completely before work:

```text
docs/vm_tlb/codex_handoff/c16/ai_workload_2239/V2C_ADOPTED_INPUT_AUTHORITY/HANDOFF.md
```

Execute V2C only.

Primary objective:

> Publish the three V2A-recovered byte-exact frozen-input payloads as a new future-use adopted input authority, with explicit provenance and without pretending the missing historical five-file bundle was recovered.

The historical decision remains `HISTORICAL_FROZEN_INPUT_NOT_RECOVERED`.

Do not run GPU/CUDA/Llama/NVBit/NCU and do not invoke a tokenizer.

Do not modify historical scientific evidence. Do not create or rename any file as the missing historical binding receipts.

On success, commit and push this branch, report the final commit and validation details, then stop.
