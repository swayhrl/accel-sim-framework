# Baseline authority recovery ref

The accepted Framework source authority for the qualified bounded baseline is:

`d64408a97d76a320a6d49468653d416e33677af8`

A dedicated remote branch named `hrl/awma-baseline-authority-d644-recovery` points exactly to that commit so node174 can fetch the historical config bytes through a normal advertised ref. This branch is provenance-only; do not execute current-model replay from it.
