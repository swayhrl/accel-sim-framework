# ChatGPT Review — 174 V2 Authority Stop Recovery

Date: 2026-09-20

## Reviewed remote facts

V2 execution branch:

`hrl/awma-repaired-hitpath-validity-174new-v2`

V2 STOP commit:

`aa303057188e4bcf7cc3119798cb90aba095f0c6`

Its direct parent is:

`b9bb4c6c356f2d98b9213d1d2ac3efdade057901`

GitHub can now read that parent commit directly.

The parent is exactly one commit ahead of the accepted repaired requalification authority:

`a7110f789a2bc6761d8885a2ca5628b4acf50f69`

Its contents are the reported V1 hit-path report/review pack and no extra scientific stage.

## 1. Authority decision

The V1 scientific/provenance authority is therefore NOT local-only.

It is remote-reachable through the V2 branch ancestry.

The missing original branch name was a ref-publication issue, not loss of the commit authority.

ChatGPT has republished the exact same commit object under a stable recovered remote ref:

`hrl/awma-repaired-hitpath-validity-174new-v1-recovered`

HEAD:

`b9bb4c6c356f2d98b9213d1d2ac3efdade057901`

No content was rewritten.

## 2. V2 STOP reclassification

The prior:

`STOP_SCIENTIFIC_V1_REMOTE_AUTHORITY_MISSING`

is reclassified as:

`AUTHORITY_REF_MISSING_RESOLVED_NO_SCIENTIFIC_CHANGE`

No experiment result is invalidated.

The V2 STOP review pack remains immutable historical evidence of the fail-closed check.

## 3. Accepted V1 contents

V1 accepted with scope:

- provenance closeout;
- recovered applied repair patch;
- hit-path timing semantic contract;
- hit-path model semantics audit;
- one P34 rebuilt 10/80 run started but only partial;
- no lookup matrix point admitted.

V1 partial run remains:

`PARTIAL_NOT_ADMITTED`

It is not promoted and not resumed unless an exact simulator checkpoint mechanism is proven.

## 4. Runtime situation

The exact old requalification binary was not recoverable.

V1 therefore rebuilt the accepted repaired source semantics.

Because V1 scratch was cleaned, the next stage may need to rebuild again unless a valid V1 rebuilt binary bundle still exists.

This is allowed engineering work because:

- source semantics are frozen by the recovered V1 patch/contract;
- the rebuilt binary is not claimed bit-identical to the historical old binary;
- one complete repaired P34 10/80 qualification must reproduce the accepted result before any lookup-latency variant is admitted.

## 5. Qualification target

Required complete P34 repaired natural 10/80:

```text
Q05 target cycles         = 1,619,068
downstream admissions     = 3,090,304
translated admissions     = 3,090,304
untranslated              = 0
unobserved                = 0
post-ready retranslation  = 0
same completed target identity
```

Any deterministic mismatch is scientific STOP.

## 6. Next stage

`AWMA_REPAIRED_HIT_PATH_MODEL_VALIDITY_174NEW_V3`

The stage now has valid remote V1 authority and should proceed directly to:

1. materialize/freeze one durable rebuilt runtime bundle if needed;
2. qualify it with complete P34 10/80;
3. run all six target-only lookup-latency points;
4. derive the model-validity envelope;
5. optional Prefill GEMM screen only after the primary matrix.

No architecture mechanism is authorized.
