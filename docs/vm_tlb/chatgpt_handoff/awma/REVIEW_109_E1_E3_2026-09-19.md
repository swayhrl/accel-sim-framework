# ChatGPT Review — 109 E1 Authority / E3 Scoped Closure

Date: 2026-09-19

Reviewed branch:
`hrl/awma-e1-authority-moe-109-v1`

Reviewed final HEAD:
`10c1c19fd0314a187a772ea83034d71a64eb569a`

Parent:
`a271a0e57d3cb61ee878e686e6e517082a9f97df`

Ancestry:
exactly one execution commit ahead of the expected 109 parent.

## 1. Decision

Stage result:

`AWMA_E1_AUTHORITY_AND_MOE_DIAGNOSTICS_109_V1_ACCEPTED_WITH_SCOPE`

E1 stop:

`REPLAY_EQUIVALENCE_FAIL_REQUIRES_REPEATABILITY_QUALIFICATION`

E3 stop:

`ROUTING_CONTROL_HOOK_NOT_YET_QUALIFIED`

These stops are valid fail-closed behavior.

Neither stop is treated as evidence that E1 or E3 is scientifically infeasible.

## 2. E1 authority accepted

Exact AWQ live module authority was created for:

- `model.layers.0.self_attn.q_proj`
- `model.layers.0.mlp.down_proj`

Runtime:

- torch 2.5.1+cu124
- CUDA 12.4
- `fuse_layers=false`

Common token authority:

`0ab5bfe82130720edcbeac23c83b44b16cd8d21e4ccd5b4ee41c465c9159b4f9`

AWQ revision:

`b25037543e9394b818fdfca67ab2a00ecc7dd641`

Accepted M256 live input authorities:

- q_proj: `[1,256,3584]`, FP16
- down_proj: `[1,256,18944]`, FP16

The module path/class is directly bound to `WQLinear_GEMM`, so the old launch-order semantic ambiguity is materially reduced.

## 3. Replay discrepancy is not yet interpretable as semantic failure

Independent reload replay versus saved live output:

- q_proj max_abs = 0.03125
- down_proj max_abs = 0.001953125

The review pack reports only max_abs and bitwise status.

It does not yet provide:

- output magnitude distribution;
- max/percentile relative error;
- exact-element fraction;
- ULP-distance distribution;
- same-instance repeatability;
- reload-to-reload repeatability;
- qweight/qzeros/scales/bias hashes across reloads;
- live-immediate-replay in the same process/module instance.

Therefore max_abs alone is insufficient to decide whether the mismatch indicates:
- wrong authority/module state;
- different loaded quantized weights;
- address/autotune-dependent numerical order;
- ordinary FP16/custom-kernel non-bitwise repeatability;
- another runtime difference.

The next stage must diagnose repeatability before any tolerance is accepted.

Do NOT simply declare 0.03125 or 0.001953125 acceptable.

## 4. Tolerance contract policy

A numerical tolerance may be created only from a separate repeatability qualification and a held-out validation set.

It must not be chosen directly from the two observed failed max_abs values.

Required factors:

1. same module instance / same input repeated calls;
2. fresh independent reloads with exact buffer hashes;
3. fresh live-hook output followed immediately by same-instance replay of the saved input;
4. held-out reloads after a repeatability envelope is frozen.

Possible accepted classification:

`NUMERICALLY_EQUIVALENT_UNDER_QUALIFIED_RUNTIME_REPEATABILITY`

only if holdout runs remain inside the frozen envelope.

Otherwise retain:

`REPLAY_EQUIVALENCE_FAIL`.

## 5. Missing raw-side closure

The new review pack contains AWQ live authorities only.

Historical raw authority exists under:

`e1d210d662ece6975648d04f628eb3f9e938117f`

but the exact q_proj/down_proj M256 live/module authority requested by the current E1 design is not packaged in the new review pack.

The next 109 stage should close raw q_proj/down_proj module authority as a bounded task rather than assume the historical layer-level replay is identical evidence.

## 6. Node164 provenance packaging gap

The report states that AWQ authority was uploaded and verified on node164.

The Git review pack does not contain a dedicated:
- RAW_DATA_INDEX.tsv
- RUN_RECEIPTS.json
- node164 ACK/receipt manifest

for those large activation authority artifacts.

The next stage should add a compact durable authority index/receipt without recreating valid data.

This is a provenance closeout, not a scientific rerun.

## 7. E3 control-hook decision

Q30 natural S2/T2048 authority remains accepted:

`ee67225edc8fc5868de585d38e0391cbeb755d9f`

The prior stop was correct because no accepted route-intervention harness existed.

However, the experimental question does not require modifying the whole model router if the exact replay exposes the MoE expert-call boundary.

The next stage may qualify a local diagnostic harness that:

1. loads the accepted exact layer/state;
2. obtains natural `routing_weights` and `selected_experts` from the real gate;
3. calls the exact accepted experts implementation directly with explicit routing tensors;
4. first proves the direct experts-call natural case reproduces the natural MoE expert output;
5. only then constructs P and U-active.

This is a diagnostic input harness, not a new architecture mechanism.

## 8. P permutation contract

Use a deterministic token-row permutation derived from the accepted state/manifest.

Jointly permute:
- hidden-state rows;
- selected_experts rows;
- routing_weights rows.

Execute the exact experts function.

Inverse-permute the output.

P passes only if it reproduces the natural expert output under an accepted numerical repeatability contract.

No route counts are changed.

## 9. U-active contract

Use the naturally active expert set only.

Let:
- M = 2048 tokens
- k = model top-k
- A = sorted naturally active expert IDs

Construct deterministic balanced selected_experts such that:

- each token has k distinct experts;
- all selected IDs are in A;
- total assignments = M*k;
- global expert counts differ by at most one where mathematically possible;
- the original per-token routing-weight vector is preserved in its k rank slots;
- no expert weight/backend/residency policy changes.

This changes model function and is explicitly:

`SYNTHETIC_ROUTING`

It is valid only as an execution/fragmentation/working-set diagnostic.

## 10. Next 109 stage

`AWMA_E1_REPLAY_REPEATABILITY_AND_MOE_CONTROL_HOOK_109_V1`

This stage is independent of the running 174 mainline.

It should:
- close node164 activation provenance packaging;
- qualify AWQ replay numerical repeatability;
- close raw q_proj/down_proj live/replay authority;
- resume E1 core only if replay authority qualifies;
- qualify the Q30 direct-experts N/P/U-active harness;
- run light E3 timing only after the hook canaries pass.

No detailed trace or new model is authorized.
