# Open issues

Determine why actual cuBLAS BF16 gemvx variant selection drifts across fresh processes without changing backend/algorithm. Formal capture requires a stable actual function fingerprint/occurrence selector.
