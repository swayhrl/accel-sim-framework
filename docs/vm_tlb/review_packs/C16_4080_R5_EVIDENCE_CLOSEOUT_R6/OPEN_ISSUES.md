# Open issues

No R5 scientific artifact-integrity gap remains.

Two non-blocking repository replay-transcript limitations remain:
- U7 exact argv / standalone metric-manifest hash / standalone target-contract receipt hash are not recoverable from the currently committed R5 evidence.
- U9 separate arm/target-binding receipt path+SHA is not recoverable from the currently committed R5 evidence.

They are documented in `PROVENANCE_LIMITATIONS.md` and do not block `READY_FOR_MULTIMODEL_REVIEW`. Multi-model characterization still requires separate authorization.
