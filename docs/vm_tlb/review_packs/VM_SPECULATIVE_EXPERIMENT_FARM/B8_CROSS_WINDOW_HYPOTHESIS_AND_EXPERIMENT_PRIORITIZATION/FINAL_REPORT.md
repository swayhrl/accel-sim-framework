# B8 final report: cross-window hypotheses and experiment prioritization

**Status: `SPECULATIVE_DIAGNOSTIC`**
**Mode: analysis-only; no B8 experiment was executed.**

## Outcome

B8 turns B7's incomplete farm and committed C4/C7 evidence into six falsifiable hypotheses (H1--H6) and an 18-bundle minimum-information set. The first recovery batch is E01--E10 only: six translation/PWC diagnostic smokes, two one-job memory calibrations, and two matched 16-kernel streaming static samples with an object-attribution audit. It is not a replay of B7's 1321-item resume list.

No result is promoted to full-workload, formal, performance, or candidate-validation status. B7 still has no `REAL_PASS` coverage. Its two `REAL_PARTIAL` cells, smoke telemetry, and partial phase mining retain their original scope. C4/C7 remain bounded candidate/opportunity evidence in a different executable context.

## Evidence-driven synthesis

1. **H1 -- phase difference.** B partial mining suggests decode has more exact local sequentiality than prefill, but kernel populations are unmatched; C adds only selected decode evidence. E09/E10 are the smallest matched static test and E15 is the first continuous phase comparison if lower gates pass.
2. **H2 -- decode PWC/PTW sensitivity.** One B Decode1 PWC-OFF smoke delta exists while most smoke points are invariant. E01--E03 distinguish finite capacity from ideal residual headroom before E11 tests sustained behavior.
3. **H3 -- Weight Segment and attribution.** C descriptor-range coverage does not prove classified traffic coverage, and B partial lanes are largely UNKNOWN. E09/E10 audit provenance first; E17 tests the candidate only in C with object and physical-counter observables.
4. **H4 -- sub-entry locality.** C static grouping is strong but C4 bounded replay shows post-L1 occupancy only 1/16, no existing-group fill, and equal standard/sub-entry final L2 results. E16 is a direct falsifier, not a confirmation run.
5. **H5 -- conventional alternatives.** PWC/TLB/page/PTW supply have more direct B paths than any candidate measurement. E01--E06 and E11--E15 let conventional mechanisms win, tie, or reject the candidate narrative.
6. **H6 -- UNKNOWN/object completeness.** B UNKNOWN mass (79.060% prefill; 90.964% decode in partial samples) and the C descriptor/proxy gap make provenance a prerequisite. It remains `NEEDS_RUNTIME_EVIDENCE` until audit and runtime observables are recorded.

## Decision value and limits

Each bundle states its evidence gap, resource gate, required observable, and positive/negative/no-difference consequence. Later recovery can end a branch after a falsifier or bounded null instead of broadening a sweep by default.

B8 cannot decide whether a candidate improves a continuous full workload, whether B and C numerical results agree, which cache/TLB grid point is optimal, or whether a Weight-targeted mechanism has a valid global attribution base. Those statements require separately gated future evidence.

## Safety and provenance record

- B8 used only committed B documents and existing B offline artifacts.
- C evidence was read only from commit `ea07cb0ec6fb3212c18f3435637f055e1296d737` through committed-object access.
- Window A and Window C private worktrees, scratch, processes, configs, binaries, logs, manifests, and resources were not accessed or changed.
- No simulator, B worker, build, trace generation, full-ROI scan, smoke rerun, or recovery experiment was started.

Auditable details are in `INPUT_PROVENANCE.tsv`, `HYPOTHESIS_MATRIX.tsv`, `EVIDENCE_CONFLICTS_AND_LIMITS.md`, and `MINIMUM_INFORMATION_EXPERIMENT_SET.tsv`.
