# Unattended operation

Run `driver.py --campaign campaign.json --resume`. Inspect state_root receipts. Resume only hash-valid PASS stages. BLOCKED is intentional for missing scientific authorization, signature ambiguity, ACK absence, or changed semantic hashes. Human judgment selects scientific targets.
