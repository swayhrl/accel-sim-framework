# Host environment

Capture host evidence: one physical four-GPU RTX 3080 Ti SM86 node, CUDA 12.6,
Python 3.10.12, torch 2.6.0+cu126, and NVBit 1.7.6. The post-capture audit uses
only main-server archives; no remaining non-regenerable evidence requires the
rented host. `SAFE_TO_POWER_OFF`.
