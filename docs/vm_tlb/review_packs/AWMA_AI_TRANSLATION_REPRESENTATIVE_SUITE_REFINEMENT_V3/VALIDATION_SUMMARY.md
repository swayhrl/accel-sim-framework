# Validation summary

1. The input SHA verifier accepts exactly the frozen V2 inventory, structural
   catalog, and Lane C asset audit.
2. All 100 structural catalog keys, counts, and accumulated durations match
   the 34,677 V2 launch rows. Their total is 154,876,910 ns.
3. The 69 generated clusters partition the same total without overlap.
   The Decode shape trajectory has 32 source step rows and 10,661,015 ns.
4. Every suite's additive `represented_mass_accounted_ns` sums to its
   independently computed frontier numerator. The trajectory is charged once.
5. No measurement launch is in the 6,897 statistical holdout launch records.
   The scientific holdout cluster is absent from every candidate tier.
6. All five accepted simulator asset strata reconcile to source V2 launch
   index, phase, full function, grid/block and step using the Lane C payload
   filename. Canonical structural keys agree.
7. Directed unit tests cover monotonic grouping, grid reversal split,
   sustained duration regime split, and unknown function key separation.
8. A second end-to-end run is byte identical for all generated artifacts.
9. `SHA256SUMS` closes the published script, tests, report, and review data.

There was no GPU capture, simulator run, mechanism run, or Lane B change.
