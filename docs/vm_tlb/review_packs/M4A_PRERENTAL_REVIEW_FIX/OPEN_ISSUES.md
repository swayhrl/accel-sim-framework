# Future M4A-C gates and stop boundary

1. A future rented host must pass host preflight and capture-ready preflight
   with explicit CUDA 12.6 `--cuda-home`; host PATH compiler values may differ
   but cannot be selected for build.
2. Generic NVBit injection, profiler ROI behavior, actual TP4/NCCL, real KV
   VA coverage, trace-size sizing, and parser compatibility require that host.
3. The paper's author-exact TP/collective/dtype/loader details remain
   `PAPER_DETAIL_UNAVAILABLE`; capture remains self-capture.

STOP for ChatGPT review. Do not rent/access a GPU, set M4A-C authorization for
a real workload, download formal weights, trace, implement Segmentation, or
inject synthetic KV.
