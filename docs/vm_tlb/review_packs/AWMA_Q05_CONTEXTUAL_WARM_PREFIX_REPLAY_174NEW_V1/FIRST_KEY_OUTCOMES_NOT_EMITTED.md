# First-key outcomes

No new first-key instrumentation was added. The accepted binary has the mandatory aggregate counters; introducing new per-key telemetry would require a separate disabled-by-default neutrality gate. This stage therefore reports aggregate Q05 deltas only, as allowed by the contract.
