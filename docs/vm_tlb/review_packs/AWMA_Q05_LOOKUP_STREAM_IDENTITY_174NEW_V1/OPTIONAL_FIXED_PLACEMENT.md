# Optional fixed placement

Not run: D6 did not show LOCAL access-count change, so the precondition for a source-safe fixed-placement diagnostic was absent. `FIXED_PLACEMENT_DIAGNOSTIC_NOT_SOURCE_SAFE` for this stage.
