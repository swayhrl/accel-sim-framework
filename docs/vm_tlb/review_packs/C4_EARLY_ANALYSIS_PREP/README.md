# C4 early analysis preparation

`C4_FINAL_STATUS = WAITING_FOR_PREFILL_PAPER`

This low-resource, no-replay package uses only the seven C3 arms that already
satisfy the terminal exit/count gate. Decode1 has a complete four-profile
comparison. Prefill contains only disabled/ideal/generic; `prefill-paper` is
excluded from every performance calculation until its original supervisor
naturally produces a terminal manifest.

The full 8-arm C4 comparison, offline locality, and closeout remain pending.
No simulator, build, trace scan, or configuration change was initiated. The
large per-kernel/window exports are external and intentionally not committed.
