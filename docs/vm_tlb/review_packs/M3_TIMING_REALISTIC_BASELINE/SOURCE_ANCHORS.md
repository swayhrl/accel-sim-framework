# M3 source anchors

The final state machine and requester accounting are anchored in Core
`src/gpgpu-sim/vm_translation.{h,cc}`; parser/config plumbing is in
`src/gpgpu-sim/gpu-sim.cc`.  Exact line-level anchors and complete path are in
the M1-M3 closeout `SOURCE_ANCHORS.md`.
