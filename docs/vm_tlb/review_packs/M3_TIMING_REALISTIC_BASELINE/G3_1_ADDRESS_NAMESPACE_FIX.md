# G3-1-RF — PTE address namespace injectivity fix

Status: `PASS — STOP FOR CHATGPT REVIEW`

## Source anchors and change

- Core repaired head: `a192e5dcb5b28b51fcae4b22fb9c985f60a4f5e9`.
- Preserved provisional parent: `8c613a356e6a146951cd59c9929046c6c4cfd856`.
- Accepted M2 execution head: `3b93e2432cbde1fcfa0eb68efc8b10d57ff3546b`.
- Framework handoff: `0ca67e7ca0c22f6352b63ff8a24471717be3dc3f`.

`radix_page_table_backend::pte_address()` now separates every
`(page-size class, level)` namespace using the fixed maximum VPN width:

```text
max_vpn_bits = 49 - log2(64KB) = 33
namespace_id = page_size_class * levels + level
slot = (namespace_id << max_vpn_bits) | vpn
```

This is a `MODELING_DECISION` for the generic replaceable backend.  It is
consistent with the pre-existing reserved-range sizing.  It changes neither
the identity-like `SimPPN=SimVPN` mapping nor M2 retry/resource semantics.

## Directed proof

`tests/vm_m3_g3_1_test.cc` now machine-checks:

- the former collision is gone: 64KB level 0 VPN `(4<<28)|0x12345` and 2MB
  level 0 VPN `0x12345` have different PTE PAs;
- min/max valid VPN values for all eight 64KB/2MB × four-level namespaces are
  inside the PTE range and strictly separated from the next namespace;
- level distinction, page-class distinction, application/PTE range separation,
  physical/non-recursive PTE request flags, and replacement-backend seam.

The complete M1/G2/M2-RF/G3-1 directed suite passes:
`/tmp/g3-1-rf-evidence/DIRECTED_TESTS.log`.

## Integrated validation

A cold Core+Framework build completed at the repaired head.  The bounded
RTX3070 one-kernel functional M2 replay exits normally in 1.47 s with 9522
cycles, one walk, one registration/wakeup, and active MSHR/PWQ/walkers all
zero.  Evidence:
`/tmp/g3-1-rf-evidence/ONE_KERNEL_M2_FUNCTIONAL.log` and `.time`.

No PTE memory request was generated by this M2 replay; this is expected
because G3-2 L2/DRAM integration is not authorized.  PTE request objects remain
physical and translation-bypassing by directed test.

## Boundary and open issue

G3-1 is repaired but needs independent review.  G3-2, PWC, real PTE
L2/DRAM traffic, Segmentation, and all later work remain unauthorized.  The
uncommitted G3-2 WIP remains preserved in Core `stash@{0}` and was neither
applied nor used as evidence.
