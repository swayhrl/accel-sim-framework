# M4A-PF review pack

Stage status: `CONDITIONAL_PASS`.

The fixup resolves the pre-rental operational ambiguity without starting M4A-C.
Route E is the preferred formal candidate: four same-node SM86 GPUs, real
TP=4, rank-0-only NVBit injection. Route A remains an approval-required
single-GPU approximation. A full-model single-GPU trace is forbidden as the
formal paper workload.

Read `SOURCE_ANCHORS.md`, `CHANGED_FILES.md`, `VALIDATION_SUMMARY.md`,
`OPEN_ISSUES.md`, and `RAW_LOG_INDEX.tsv` in that order.
