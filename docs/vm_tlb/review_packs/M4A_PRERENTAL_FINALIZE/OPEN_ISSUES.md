# Conditional M4A-C host gates and stop boundary

1. The rented host must pass the separate host and capture-ready preflights:
   four same-model SM86 GPUs, >=12 GiB each, >=500 GiB free/expandable local
   storage, sufficient host RAM, SSH/copy-back, locked Python/runtime/toolkit.
2. Generic NVBit injection, profiler ROI behavior, actual TP4/NCCL, real KV
   VA/lifetime coverage, trace size, and parser compatibility require that
   future host. Raw trace retention avoids recapture for a later NCCL decision.
3. Author-exact TP implementation/dtype/loader/collective policy remain
   `PAPER_DETAIL_UNAVAILABLE`; capture is labelled self-capture.

STOP: do not rent, set M4A-C authorization for a real workload, download the
formal model, trace, implement Segmentation, or inject synthetic KV.
