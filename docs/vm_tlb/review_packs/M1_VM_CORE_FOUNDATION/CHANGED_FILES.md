# Changed files

Core: `abstract_hardware_model.*`, `gpu-sim.cc`, `shader.*`, new
`gpgpu-sim/vm_core.h`, and `tests/vm_core_m1_test.cc`.

Framework: M1 specifications, this track report, and M1 review evidence only.
