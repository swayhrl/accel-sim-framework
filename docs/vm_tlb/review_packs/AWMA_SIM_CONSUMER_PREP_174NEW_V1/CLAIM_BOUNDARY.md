# Claim boundary

This checkpoint claims only that the 174-new consumer is ready to receive and
fail-closed validate a future simulator-native producer bundle, and that the
accepted simulator baseline identity is frozen for
`HASH_BOUND_FIXED_WINDOW_10000`.

It does not claim current-model simulation, full ROI, historical-binary
identity, Native-versus-Simulation numerical agreement, trace conversion,
global ordering inferred from shards, or any TLB/segment/cache mechanism
sweep. C16WARP1/MREF remains Native evidence and was used only to bind the
expected workload and target contract.
