# Future high-fidelity requests — new authorization required

No request below is launched automatically.  Each requires a fresh user
authorization after the stated evidence and cost controls are available.

1. Wave-1 native baseline and lightweight kernel census after C16-0.9 package
   closure and a G resource receipt; retain the frozen S0–S4 identity and all
   warmup/measurement receipts.
2. Bounded NCU representative-counter capture only after C's fixed selection
   release, metric availability preflight, and a target-identity receipt.
3. Bounded NVBit capture only after C/H's fixed target plan, trace size/time
   guards, and a costed transfer plan; do not expand targets because a GPU is
   idle.
