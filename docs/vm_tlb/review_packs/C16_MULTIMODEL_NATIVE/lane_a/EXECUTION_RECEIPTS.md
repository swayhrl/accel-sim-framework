# C16 A local execution receipts

| Receipt | Command or method | Result | Cost/evidence boundary |
| --- | --- | --- | --- |
| C15 static selftest | `static_fingerprint.py --selftest` | T03/T04/T05/T06 PASS | CPU-only fixture/static validation |
| C15 static validate | `static_fingerprint.py --validate .../C15.../lane_a` | T01/T14/T20/T18/T22 PASS | CPU-only schema/provenance validation |
| C15 units | `python3 -m unittest tests/vm_tlb/c15/lane_a/test_static_fingerprint.py` | 11 PASS | CPU-only synthetic tests |
| C15 manifest closure | Git object reads plus SHA-256 recomputation | A 34, B 21, C 35 payloads match | read-only artifact commits only |
| C16 selftest | `c16_local_prep.py --selftest` | C16A_T01 PASS | CPU-only |
| C16 assets | `c16_local_prep.py --record-assets` | C16A_T02 PASS; 79 manifest rows | metadata/tokenizer downloads and remote LFS declarations only |
| C16 tokenization | `c16_local_prep.py --tokenize` | C16A_T03 PASS; 84 receipts | CPU-only `transformers==4.51.3`; no model load |
| C16 validate | `c16_local_prep.py --validate` | C16A_T04 PASS | local hashes and token derivations |
| C16 units | `python3 -m unittest tests/vm_tlb/c16/lane_a/test_c16_local_prep.py` | 4 PASS | CPU-only unit tests |
| C16 Wave-1 integrity retry | Two complete multi-range Qwen2.5-0.5B transfers, then SHA-256 comparison to the immutable `7ae5576…` LFS declaration | Both complete files rejected: expected `fdf756fa…`; retained quarantine copies hash to `5cc160aa…` and `cfc482c0…`. A fresh serial transfer is in progress and is not yet an accepted asset. | Local network/disk only; no model load, GPU, profiler, NVBit, simulator, SASS, or full-ROI work. |
| C16 fixed-input revalidation (C) | Rehash each payload in C commit `29e669eca19ac3b2a1350bf2d097569a41f123e1` against its manifest | 24/24 PASS; candidate remains non-dynamic offline selector protocol only. | Read-only Git-object validation. |
| C16 fixed-input revalidation (H) | Rehash H code payloads and test receipt in commit `932c6fa44a4896265214fc2136e34698402a5c7f` | 4 code payloads plus test receipt PASS; candidate remains non-dynamic memory protocol only. | Read-only Git-object validation. |
| C16 fixed-input revalidation (G) | Resolve every path named by G commit `57012031a017e8b42977dad5d8f7692cc6521080` manifest | REJECTED: manifest names `EXECUTION_BUDGET_GUARD.md`, absent at its declared release root. | Read-only Git-object validation; no live partial substitution. |
| C16 fixed-input revalidation (G replacement) | Resolve every path named by newest G commit `de7b3e33f2bb7ae6b4195c92ecd6201dd3274738` manifest | REJECTED: its 17-payload manifest still names `EXECUTION_BUDGET_GUARD.md`, absent at its declared release root. | Read-only Git-object validation; no wheel, environment, runner, or live partial content consumed. |
| C16 final fixed-input revalidation (G/C/H) | Rehash every G release payload at `G@45e293b84940ef59b7b134bcda48aca7d0b99b2f`, every local wheel named by its wheelhouse manifest, all C payloads at `C@29e669eca19ac3b2a1350bf2d097569a41f123e1`, and H code/test payloads at `H@932c6fa44a4896265214fc2136e34698402a5c7f` | PASS: G 17/17 release payloads and 66/66 wheels, C 24/24 payloads, H 5/5 code/test payloads; manifest SHA-256 values are `7c18c2a8…`, `14a7c029…`, and `b7821231…`, respectively. | CPU-only file/Git hashing. G/C/H are now final-consumed only as offline infrastructure/protocol artifacts; no dynamic scientific result is inferred. |
| C16 GPU-package generator static validation | `py_compile`, C16A_T01, four unit tests, and direct `validate_final_fixed_releases()` through the A package generator | PASS: generator itself revalidated G/C/H bindings and all 66 G wheels. It was not asked to write formal package artifacts while C16A_T06 remains open. | CPU-only source, Git-object, and file hashing; no model import, GPU, profiler, NVBit, simulator, SASS, or full-ROI action. |
| C16 rolling package P0 | `c16_local_prep.py --write-rolling-package C16_GPU_PACKAGE_P0 c16_llama32_1b_frozen_compatible` followed by `--validate-rolling-package C16_GPU_PACKAGE_P0` | PASS: Llama-only P0 wrote and validated `C16_GPU_PACKAGE_MANIFEST.tsv`, `EXPECTED_HASHES.tsv`, `TRANSFER_PLAN.md`, package manifest SHA-256 `ac59f0d2…`, and immutable package identity. It includes no temporary Qwen transfer file. | CPU-only file/Git hashing; package preparation only, no transfer/import/GPU/profiler/NVBit/simulator/SASS/full-ROI execution. |
| C16 rolling package P1 | `c16_local_prep.py --write-rolling-package C16_GPU_PACKAGE_P1 c16_qwen25_05b_native_reference` followed by `--validate-rolling-package C16_GPU_PACKAGE_P1` | PASS: Qwen2.5-0.5B-only P1 is immutable and its package manifest SHA-256 is `d8ac3ca4…`. It includes no raw/AWQ temporary shard. | CPU-only file/Git hashing; package preparation only, no transfer/import/GPU/profiler/NVBit/simulator/SASS/full-ROI execution. |
| C16 Wave-2 DeepSeek-V2-Lite asset closure | `c16_local_prep.py --record-assets`, local file SHA-256 compared with immutable revision `604d5664…` LFS declarations | PASS: 4/4 checkpoint files, `31,413,626,576` bytes total, local paths and tokenizer revision recorded. | CPU-only file hashing and Hub metadata reads; optional Wave-2 preparation, not a C16-0.9 gate. |
| C16 Wave-1 Qwen2.5-0.5B accepted download | Whole-file SHA-256 after byte-range assembly, compared with immutable revision `7ae5576…` LFS declaration | PASS: `model.safetensors`, `988,097,824` bytes, `fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe`. | CPU/network/disk only; validated temporary range slices were removed after assembly, while prior mismatching full attempts remain quarantined. |
| C16 Wave-1 raw Xet fallback probe | `hf_hub_download` for raw shard 3 at immutable revision `a09a354…` | STOPPED_NO_PROGRESS: no local checkpoint or cache bytes materialized after one minute; range download remains the active method. | CPU/network control-plane only; no model file accepted or manifest content changed by the attempt. |
| C16 Wave-1 transport hardening | Replaced overwrite-on-retry range staging with one `curl --continue-at -` stream per raw/AWQ shard | ACTIVE: all six temporary files retain their existing prefix and grow monotonically; a whole-file SHA-256 is still required before acceptance. | CPU/network/disk only; temporary files remain excluded from the manifest. |
| C16 Wave-1 Qwen2.5-7B-AWQ shard acceptance | Whole-file byte-count and SHA-256 validation after the pinned `curl --continue-at -` transfer of `model-00002-of-00002.safetensors` | PASS: `1,574,406,784` bytes and `920a8cc9d3c81668f18014a69bb041ab8bab22a31640a713f90f5e7580e2f126`, exactly matching immutable revision `b25037543…` LFS metadata. The accepted file was renamed out of its temporary suffix. | CPU/network/disk only; AWQ shard 1 and all raw shards remain unaccepted pending their own complete whole-file checks. |
| C16 Wave-1 local gate intermediate check | `c16_local_prep.py --validate-wave1-local` after accepting AWQ shard 2 | EXPECTED FAIL: four raw checkpoint rows and AWQ shard 1 remain remote-only in the manifest, so C16A_T06 cannot close C16-0.2 yet. | CPU-only manifest/path validation; the failure is retained as an explicit gate, not masked as a pass. |
| C16 Wave-1 staging cleanup | Verified no `*.safetensors.range.*` consumer remained, then removed the abandoned range fragments | 13 temporary fragments, `104,393,344` bytes removed; no accepted checkpoint, manifest payload, or active download target removed. | Recoverable only by re-download; source bytes were unaccepted staging and the decision is logged here. |
| C16 Wave-2 Qwen3-30B-A3B start guard and worker | Checked filesystem free space before launch, then started the separate serial pinned-revision HTTP/1.1 continue worker | PASS: at `2026-09-12T14:51:10Z`, `95,164,334,080` bytes available exceeded the revised 85 GiB (`91,268,055,040`) launch gate. An initial non-isolated wrapper was session-cleaned before it wrote a byte; the session-isolated worker then began shard 1 of the frozen 16-file, `61,066,575,648` byte set at revision `ad44e777…`, caps itself at 1 MiB/s, and pauses below 15 GiB. | CPU/network/disk only; `.incomplete` remains unaccepted until whole-file byte-count and SHA-256 equality. Worker neither interrupts Wave-1 nor alters P0/P1, and does not start GPU/profiler/NVBit/simulator/SASS/full-ROI work. |
| C16 Wave-1 completed-shard finalizer | `c16_wave1_shard_finalizer.py --once` before launching its separate observer | C16A_T11 PASS: all five unresolved raw/AWQ files were still named by live curl processes and therefore remained `TRANSFER_ACTIVE_UNACCEPTED`. The watcher may atomically promote a file only after its owning curl exits and exact frozen byte count plus SHA-256 match. | CPU/disk observation only; it has no start, stop, retry, or restart operation for a Wave-1 worker and records its own local-only progress receipt. |
| C16 asset-download strategy telemetry and single-SHA finalizer upgrade | C16A_T11 single-SHA policy check, `c16_download_progress.py --once`, and a low-I/O-priority finalizer observer | PASS: six active workers are recorded in local `DOWNLOAD_PROGRESS.tsv`; P2/P3 identity reservations and immutable receipt consumption are source-validated by C16A_T13. | No Wave-1 curl was stopped, restarted, or reconfigured. Telemetry cannot restart a worker; its 15-minute inactive/no-growth state is only a future operator recovery gate. |
| C16 Wave-1 raw shard 2 immutable closure | Existing revision-pinned curl exited; finalizer performed exact-size check then one whole-file SHA-256 before promotion | PASS: `Qwen/Qwen2.5-7B-Instruct@a09a354…`, `model-00002-of-00004.safetensors`, `3,864,726,352` bytes, SHA-256 `f5d25a2772cb825164a2a2c0fb6d51a87e282abf21e4dd75bc5cfb3cd0ea6185`; local receipt records immutable verification. | CPU/disk verification at idle I/O priority; the other three raw shards and AWQ shard 1 remain independent unaccepted transfers, so P2 remains blocked. |

No GPU, CUDA/Torch model load, nsys, NCU, NVBit, Accel-Sim, SASS, or full-ROI
operation occurred in these receipts.
