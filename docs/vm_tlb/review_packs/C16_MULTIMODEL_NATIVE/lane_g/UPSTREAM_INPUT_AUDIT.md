# C16 Lane G fixed upstream-input audit

A fixed commit `2a06944c` manifest verified 114 payload hashes. It supplies hash-bound model metadata, frozen input/token receipts, and scenarios, but remains a partial local-preparation publication: it does not authorize a GPU transfer or rental.

C fixed commit `fed28d81` manifest verified 22 payload hashes. Its selector is consumed only as a protocol; every current NVBit row explicitly awaits a committed G native catalog, so it authorizes no capture target.

H fixed commit `65b53574` has no C16 publish manifest at its documented review-pack path and is not consumed. No live partial or raw address data was read.
