# AWMA 109 unattended capture campaign V1 — early close

Status: `AWMA_109_UNATTENDED_CAPTURE_CAMPAIGN_V1_COMPLETE_WITH_SCOPE`

Reason: `USER_EARLY_CLOSE_AFTER_P2C_TO_RELEASE_NODE109`.

Completed immutable child bundles with node164 ACK: **16**. P2D and all later queue entries are explicitly skipped; no results are inferred for them.

The footprint matrix is CPU-only and consumes only accepted raw memory-event fields. It is not an Accel-Sim run or a TLB/cache result.
