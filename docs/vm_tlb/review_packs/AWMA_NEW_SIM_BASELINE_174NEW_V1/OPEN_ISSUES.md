# Open issues

The accepted baseline is `NEW_SIM_BASELINE_V1_QUALIFIED` only for
`HASH_BOUND_FIXED_WINDOW_10000`. Historical binary identity, full-ROI replay,
exact historical numerical equivalence and whole-model equivalence remain
unsupported.

No current-model simulator-native capture was attempted in this baseline
stage. That producer bundle remains a future, separate dependency and must not
be synthesized from C16WARP1/MREF Native evidence.
