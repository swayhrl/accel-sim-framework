# Semantic patch audit

No simulator semantic code change. Build-only dependency wiring: private CUDA, zlib, m4, bison, flex, and GL runtime. Consumer contract adds required sync_control semantics only.
