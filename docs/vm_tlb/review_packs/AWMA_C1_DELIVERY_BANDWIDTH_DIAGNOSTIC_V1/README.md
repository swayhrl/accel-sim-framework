# AWMA C1 delivery-bandwidth diagnostic V1

Start with `REPORT.md`. `DIAGNOSTIC_MATRIX.tsv` contains the reused one-slot
evidence and new two-slot results. `CORRECTNESS_GATES.tsv`,
`RUN_RECEIPTS.json`, `RAW_DATA_INDEX.tsv`, and `SHA256SUMS` provide the
machine-checkable evidence and provenance.

This is a bounded causal diagnostic on discovery targets, not parameter
tuning, independent validation, baseline promotion, or a novelty claim.
