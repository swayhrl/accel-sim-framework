# Determinism decision

`PREVIOUS_STREAM_TELEMETRY_ARTIFACT`. All 140672 canonical GLOBAL instructions are `INPUT_SAME_OUTPUT_SAME` between 10/80 and 0/80; generation delta is zero. The prior 88062 GLOBAL delta was not present at the actual generation boundary. No simulator correctness defect is proven, and no baseline is changed. Lookup-latency sensitivity remains qualitative simulator evidence only.
