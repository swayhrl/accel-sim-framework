# Canonical trace identity contract

The diagnostic identity is `(logical trace CTA ordinal, trace warp ordinal within CTA, trace instruction ordinal)`. CTA xyz is recovered from the kernel grid linearization where needed. It intentionally excludes timing-order `inst_uid`, SM, and hardware warp slot.
