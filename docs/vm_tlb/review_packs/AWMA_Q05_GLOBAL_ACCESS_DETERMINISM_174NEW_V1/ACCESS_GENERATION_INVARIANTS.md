# Access-generation invariants

The trace-side observer ran synchronously around `generate_mem_accesses()`. Every canonical GLOBAL instruction appears exactly once; no duplicate generation or cross-run missing identity was observed.
