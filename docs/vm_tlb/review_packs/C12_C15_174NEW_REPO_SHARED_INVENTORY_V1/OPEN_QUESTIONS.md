# Open questions for joint review

- Can old174 archaeology prove which `/root/share/workspace_migrated_20260905` trees are C12–C15 outputs, and bind each to exact source/config/core/binary/trace identity?
- Are the visible M4B/M4C logs byte-equivalent to the raw logs named by Git review packs, or only related by directory naming?
- Is `/root/data` mounted/shared on old174 even though it is empty here?
- Which historical outputs, if any, are old-container-private and must be copied (not moved) to node164?
- Does any producer outside the searched repository emit simulator-compatible `.traceg.xz` from modern C16 captures?
- Can C16 active-lane records reconstruct warp/coalesced transaction boundaries without scientific ambiguity?
- Are historical object-map ranges globally monotonic, satisfying the analyzer's binary-search precondition?
- Which C12/C13/C14/C15 documents are formal versus diagnostic/pre-fix/obsolete after old174 source archaeology?
- Which accepted Core/framework SHA should be used for a future compatibility smoke, and can the current binary be rebuilt reproducibly?
