# Downstream capture scope decision

Decision: no additional GPU collection is authorized by this H analysis.

The admitted Recovery-V2 evidence closes the requested selected-target S0
fingerprint and supplies Decode2/3/4 cross-step set relations.  A future
request that needs model-wide behavior, temporal reuse, object attribution,
physical mapping, TLB/cache measurements, or another selected instruction must
be separately scoped and must not be inferred from this pack.

For any future collection, retain the same immutable publication and raw SHA
closure, state the new selected target, and provide a direct runtime object-map
receipt if object attribution is desired.
