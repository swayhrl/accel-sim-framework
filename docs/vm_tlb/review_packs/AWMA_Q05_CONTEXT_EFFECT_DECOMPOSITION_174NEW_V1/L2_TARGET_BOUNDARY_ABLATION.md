# L2 target-boundary ablation

`L2_TARGET_BOUNDARY_ABLATION_NOT_SAFE_OR_NOT_NEEDED`.

The accepted source exposes global/kernel-completion flush policy, not a source-safe one-shot Q05-boundary L2 invalidate that is demonstrably independent of translation state and dirty-line correctness. This optional diagnostic is therefore not attempted. F0 `flush_l2` remains unchanged.
