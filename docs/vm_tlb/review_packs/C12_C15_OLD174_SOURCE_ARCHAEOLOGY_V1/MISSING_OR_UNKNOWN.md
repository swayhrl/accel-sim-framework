# Missing or unknown boundary

The following is unknown, not absent: the contents of old174 Docker-private
`/workspace`, `/root` outside verified mounts, `/tmp`, and any Docker overlay.
The old174 SSH daemon responded, but no provided identity authenticated.
No password guessing, data mutation, cleanup, or GPU activity was attempted.

The C12 raw-log index proves historical path names and SHA-256 identities in
Git, but it cannot prove present old174 retention.  C14 similarly names
micro-run directories in Git, but those paths remain uninspected.  A future
read-only pass needs the authorized old174 credential, then must collect
mountinfo, path existence, `du`, file counts, and SHA-256 before deciding
whether any item is `OLD_DOCKER_PRIVATE_MUST_MIGRATE`.

Do not treat the current container's overlay `/workspace` as evidence about
old174.  Do not duplicate `/root/share` or `/root/data` merely because
old174 visibility is not independently proven.
