# Changed files

The stage adds only Framework documentation, static validation tooling, the
prepared capture wrapper, and review evidence.  It changes no
`gpu-simulator/` file and no Core repository file.

| Area | Purpose |
|---|---|
| `docs/vm_tlb/llm/` | workload, hardware, metadata, artifact, local-inventory, runbook, and legacy-audit contracts |
| `util/llm_trace_capture/` | static contiguous-layout and sidecar validators; M4A-C preflight/driver protected by authorization gate |
| `docs/vm_tlb/review_packs/M4A_PRECAPTURE_PREP/` | this review evidence |
| `docs/vm_tlb/codex_handoff/m4a/` | track-specific closeout report |
