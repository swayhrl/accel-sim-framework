# Commit history

## Inputs

- Core frozen base: `73774727` — `Fix ptxinfo parser for CUDA 11+ ptxas resource-usage output (#138)`
- Framework frozen base: `3016c658` — `Fix kernel attribute query when cuLaunchKernel uses CUkernel handles (#521)`

## S1-B0 output

The Framework commit produced by this bootstrap adds only the explicit
`docs/vm_tlb` paths enumerated in `CHANGED_FILES.md`. Core has no S1-B0 commit,
because it has no source changes and no verified writable remote.
