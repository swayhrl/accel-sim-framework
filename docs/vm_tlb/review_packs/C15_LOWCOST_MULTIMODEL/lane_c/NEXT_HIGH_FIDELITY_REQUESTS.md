# Future high-fidelity requests — not authorized to execute

- **T3-1** — matched cold/full C12-style kernel windows; accept only if identity receipt and state comparability pass. `requires_new_authorization=true`.
- **T3-2** — one B deployment semantic strata missing from cheap directory; accept only if selector coverage gaps close without oracle fields. `requires_new_authorization=true`.
- **T3-3** — bounded exact page+line read/write/atomic fingerprint; accept only if exact-small calibration meets frozen error threshold. `requires_new_authorization=true`.
