# Mechanism sign audit

All 384 rows use the same frozen sample plan on reference and candidate arms. They are historical retrospective tests. 381 rows are INCONCLUSIVE because the deterministic selector has no defensible sampling interval or its empirical resolution is at least the effect. C13 capacity rows are separately tagged `CONFOUNDED_CAPACITY_AND_SEGMENT_NOT_SINGLE_VARIABLE`; no lookup-latency conclusion is made from them.
