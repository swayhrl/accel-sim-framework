# AWMA 174 minimal requalified cross-target hitpath V2

Scope-limited closeout: T0 has three admitted isolated repaired points; T1/T2 are `TARGET_NOT_ADMITTED` because their producer bundles lack target-specific VM map bindings. Classification: `INSUFFICIENT_CROSS_TARGET_EVIDENCE`. Simulator-model validity only; no hardware-latency or mechanism claim.
