# Blocker

The one-MREF raw buffer tracer yields zero records on some selectors and exceeds a five-minute bound on a known-positive CTA100/static810 path. This prevents required terminal/drop/overflow/raw-shard closure.
