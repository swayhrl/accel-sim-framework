# 第1轮：从已有合并能力转向剩余服务与决策边界

日期：2026-09-26；执行者：ChatGPT。**这是文献支线交付，不是Lane E结果，也不改变其Goal。**

## 已完成

本轮建立9篇笔记：2篇用户提供全文的整理/复查（Memento、LATPC），5篇取得一级来源正文并核关键章节（SIMT walk scheduling、Neighborhood、MASK、Virtual Caching、Core Partitioning），2篇仅作者摘要/介绍（Marching Page Walks、Avatar）。不将后两篇算作全文阅读，不将旧成果整理全部称为本轮新发现。

新深入的内容主要是Neighborhood、Virtual Caching与Core Partitioning；同时把此前散落在对话/协调文件中的结论整理成固定paper ID、原文定位与适用边界。

## 本轮最有用的比较认识（分析，不是新实验）

1. **不能只盯“合并”：**同VPN去重、PTE-line共享、同指令walk调度、多VPN MSHR压缩解决不同工作，不能拿一种失败直接否掉另几种。
2. **不能只看TLB本身：**Virtual Caching提供数据驻留与翻译需求的联合视角；MASK提供翻译/数据争用视角。它们是已有能力与问题，非自动成为我们的新方案。
3. **不能只看issue优先级：**RBA与subcore assignment说明请求服务、调度、工作归属的决策层级不同。若问题位于别层，继续修改TLB可能无效。
4. **不能把小结构或hint当创新本身：**Memento的价值在旧方案与新硬件假设之间的具体失配，同时存在profile、时序与容量成本；不是“自适应+AI”两个标签。

## 与当前AWMA的衔接

本笔记库基于已核协调parent `d1b58f8a4a0beac3f9c760f5933faf7e701a1152`（tree `10cf8a2ed730e9388c0834899c96c6d513c42908`）。历史PREL1非新机制定位及经典warp去重参考不改。用户报告Lane E正在执行；本輪不读取其活动工作区，不声称知道其尚未报告的新结果。

Lane E结果出来后的检索入口：
- L1-hit/端口阻塞：先看请求生成和真实资源合同；不能用miss侧论文直接代替对照。
- 多页指令/PWQ：L001→L002→L007，必要时补L008全文。
- 翻译/数据相互干扰：L003/L004；先检查语义与缓存体系适配。
- 推测翻译：先补L009的完整校验协议。
- 非翻译operand/子核问题：L005/L006；只作为相关线索，不自动转移主线。

## 未做

没有新GPU capture、模拟、机制改动、模型下载、RTL/PPA或训练。没有给Lane E新增任务。没有复制作者PDF到仓库。没有宣布新颖性通过，没有证明哪篇论文的性能能在AWMA复现。

## 下一轮原则

在后续对话中根据真实剩余问题追加阅读。优先补影响结论的原文，不为扩清单泛读；不承诺当前会话结束后自动后台持续工作。用户不需要为本轮另开Codex窗口。
