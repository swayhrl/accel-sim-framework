# AWMA Simulation Foundation Consumer Utilities

CPU-only fail-closed consumer contract for `SIM_COMPAT_CAPTURE_V1`: canonical IDs, hash-closed admission, telemetry normalization, and immutable catalog entries. C16WARP1/MREF is rejected as `NOT_PROVEN_LOSSLESS`; trace semantics are never inferred.
