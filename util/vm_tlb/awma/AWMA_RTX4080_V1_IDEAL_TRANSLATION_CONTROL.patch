--- a/src/abstract_hardware_model.h
+++ b/src/abstract_hardware_model.h
@@ -832,6 +832,7 @@
   M4C_TRANSLATION_L2_TLB_HIT,
   M4C_TRANSLATION_PTW,
   M4C_TRANSLATION_SEGMENT_HIT,
+  M4C_TRANSLATION_IDEAL_FUNCTIONAL,
   M4C_TRANSLATION_OUTCOME_COUNT
 };
 
--- a/src/gpgpu-sim/memory_telemetry.cc
+++ b/src/gpgpu-sim/memory_telemetry.cc
@@ -366,7 +366,8 @@
 const char *m4c_memory_telemetry_translation_outcome_name(unsigned outcome) {
   static const char *names[] = {"UNOBSERVED", "VM_DISABLED",
                                 "IDEAL_IDENTITY", "L1_TLB_HIT",
-                                "L2_TLB_HIT", "PTW", "SEGMENT_HIT"};
+                                "L2_TLB_HIT", "PTW", "SEGMENT_HIT",
+                                "IDEAL_FUNCTIONAL"};
   assert(outcome < M4C_TRANSLATION_OUTCOME_COUNT);
   return names[outcome];
 }
--- a/src/gpgpu-sim/vm_translation.h
+++ b/src/gpgpu-sim/vm_translation.h
@@ -173,7 +173,8 @@
   TRANSLATION_SOURCE_L1_TLB_HIT,
   TRANSLATION_SOURCE_L2_TLB_HIT,
   TRANSLATION_SOURCE_PTW,
-  TRANSLATION_SOURCE_SEGMENT_HIT
+  TRANSLATION_SOURCE_SEGMENT_HIT,
+  TRANSLATION_SOURCE_IDEAL_FUNCTIONAL
 };
 
 // C10-A carries request access intent into the candidate path.  Existing
--- a/src/gpgpu-sim/vm_translation.cc
+++ b/src/gpgpu-sim/vm_translation.cc
@@ -2221,6 +2221,21 @@
   if (source != 0) *source = TRANSLATION_SOURCE_UNOBSERVED;
   translation_key key(asid, vm_core::vpn(sim_va, m_config.page_size),
                       m_config.page_size);
+  const char *ideal_control_text =
+      getenv("GPGPUSIM_VM_IDEAL_TRANSLATION_CONTROL");
+  const bool ideal_control = ideal_control_text != NULL &&
+      unsigned(strtoul(ideal_control_text, NULL, 10)) != 0;
+  if (ideal_control) {
+    // Resolve the current functional page-table mapping only at this head
+    // admission. No lookup, MSHR, walker, PWC, PTE, cache, or queue state is
+    // read or modified; no later access is inspected or made ready early.
+    const uint64_t ppn = m_page_table->resolve_ppn(key);
+    *sim_pa = ppn * key.page_size +
+              vm_core::page_offset(sim_va, key.page_size);
+    if (source != NULL)
+      *source = TRANSLATION_SOURCE_IDEAL_FUNCTIONAL;
+    return READY;
+  }
   // Register the ASID on first admission.  A later global shootdown must
   // advance every ASID that can have an in-flight lookup, including one whose
   // current value would otherwise be the implicit default generation.
--- a/src/gpgpu-sim/shader.cc
+++ b/src/gpgpu-sim/shader.cc
@@ -2514,6 +2514,11 @@
   const char *pipeline_text = getenv("GPGPUSIM_PIPELINED_ACCESSQ_TRANSLATION_LAUNCH");
   const bool pipelined_launch = pipeline_text != NULL &&
       unsigned(strtoul(pipeline_text, NULL, 10)) != 0;
+  const char *ideal_control_text =
+      getenv("GPGPUSIM_VM_IDEAL_TRANSLATION_CONTROL");
+  const bool ideal_control = ideal_control_text != NULL &&
+      unsigned(strtoul(ideal_control_text, NULL, 10)) != 0;
+  assert(!ideal_control || m_config->gpgpu_vm_mode == 2);
   assert(!ready_application_v2 || pipelined_launch);
   const bool ready_application_diagnostics =
       getenv("GPGPUSIM_READY_APPLICATION_DIAGNOSTICS") != NULL;
@@ -2552,7 +2557,7 @@
   // Opt-in diagnostic: launch translation for resident coalesced accesses
   // without consuming/reordering accessq or admitting any access downstream.
   // Existing controller port/inflight/ready semantics remain authoritative.
-  if (pipelined_launch && m_config->gpgpu_vm_mode == 2 &&
+  if (!ideal_control && pipelined_launch && m_config->gpgpu_vm_mode == 2 &&
       m_gpu->vm_translation() != NULL) {
     std::list<mem_access_t> &entries = inst.accessq_entries();
     for (std::list<mem_access_t>::reverse_iterator it = entries.rbegin();
