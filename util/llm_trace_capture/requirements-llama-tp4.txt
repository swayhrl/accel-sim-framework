# Pin set for the prepared workload wrapper.  The model revision is supplied
# separately as M4A_MODEL_REVISION and must be an immutable HF commit SHA.
# Install torch from the CUDA 12.6 wheel index first; this records the runtime
# build separately from the local CUDA toolkit used to build NVBit.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.6.0
transformers==4.51.3
accelerate==1.6.0
safetensors==0.5.3
huggingface_hub==0.30.2
