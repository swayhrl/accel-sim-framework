# WBQ diagnostic common overlay: leave exactly one L2-to-DRAM FIFO entry
# available for a dirty victim while keeping the lower-read reservation alive.
# Use only together with a deliberately small dirty L2 geometry; this is a
# pressure test, not a representative V100 configuration.
-gpgpu_dram_partition_queues 64:2:64:64
