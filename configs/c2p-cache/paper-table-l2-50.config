# Paper Section 3 sensitivity point used only to classify the selected replay
# traces.  It changes the shared-L2 path from the Table-1 200 cycles to 50
# cycles; all other paper-table parameters stay unchanged.
-gpgpu_l2_rop_latency 50
