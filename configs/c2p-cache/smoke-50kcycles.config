# Bounded replay used only to prove that a trace can be parsed, scheduled, and
# execute instructions with the selected simulator/configuration pair.  It is
# not a performance point.  A cycle cap is intentional: -gpgpu_max_cta
# repeatedly rebinds shader cores for some SASS traces and makes this smoke
# needlessly noisy.
-gpgpu_max_cycle 50000
