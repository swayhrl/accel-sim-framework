# Same binary/configuration family as the adaptive C2P+ experiment, but keep
# exhaustive confirmation.  This is the only valid timing control for the
# policy: the separate target-tag pipe remains enabled.
-c2p_cache_separate_target_tag_port 1
-c2p_cache_max_candidate_probes 0
-c2p_cache_adaptive_probe_policy 0
