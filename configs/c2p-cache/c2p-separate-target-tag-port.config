# C2P+ counterfactual: target tags use a one-request-per-cycle pipeline that
# preserves the existing seven-cycle remote-tag latency but does not reserve
# the target L1 data port.  It is not the canonical paper C2P configuration.
-c2p_cache_separate_target_tag_port 1
