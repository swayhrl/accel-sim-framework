# Global C2P remote-tag latency sensitivity point.  This deliberately changes
# only the target tag-lookup transport from the canonical seven cycles to
# fourteen.  `c2p.config` continues to pin the remote return at two cycles,
# the shared-L2 path at 200 cycles, and every queue/probe setting.
-c2p_cache_remote_tag_latency 14
