# RING CCN queue-capacity sensitivity.  The original CCN uses an eight-entry
# ReqQ per SM; this global-controller approximation exposes 512 accepted slots
# for the paper's 64-SM topology.  It is a sensitivity point, not a C2P knob.
-c2p_cache_query_queue_size 512
