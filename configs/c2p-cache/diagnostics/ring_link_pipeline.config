# RING diagnostic: retain Table-1 hop=2 and copied-tag lookup=7, but model
# directed-link pipelining instead of a single chip-global injection source.
# This is an A/B configuration, not yet part of the primary comparator point.
-c2p_cache_ring_link_pipeline 1
