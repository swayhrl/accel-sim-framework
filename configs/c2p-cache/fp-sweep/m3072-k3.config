# Figure-13 sweep point: 64 * (32 BF + 16 tag-mask) = 3,072 logical rows.
# k includes one tag-mask encoding plus two Bloom-filter hashes.
-c2p_cache_snapshot_bf_rows_per_bank 32
-c2p_cache_bf_hashes 2
