# Figure-13 sweep point: 64 * (16 BF + 16 tag-mask) = 2,048 logical rows.
# k includes one tag-mask encoding plus one Bloom-filter hash.
-c2p_cache_snapshot_bf_rows_per_bank 16
-c2p_cache_bf_hashes 1
