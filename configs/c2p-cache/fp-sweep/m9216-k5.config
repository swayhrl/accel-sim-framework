# Figure-13 sweep point: 64 * (128 BF + 16 tag-mask) = 9,216 logical rows.
# k includes one tag-mask encoding plus four Bloom-filter hashes.
-c2p_cache_snapshot_bf_rows_per_bank 128
-c2p_cache_bf_hashes 4
