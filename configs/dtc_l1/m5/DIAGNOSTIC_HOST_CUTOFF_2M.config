# SIM_HOST-only diagnostic overlay; never a formal M5 identity.
# Source: src/gpgpu-sim/gpu-sim.cc registers -gpgpu_max_cycle; active() stops
# timing at the deterministic threshold.  The common cutoff enables A0--A3
# observer-cost equivalence comparisons without changing architecture knobs.
-gpgpu_max_cycle 2000000
