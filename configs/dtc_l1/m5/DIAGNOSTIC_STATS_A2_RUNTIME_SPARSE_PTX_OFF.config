# SIM_HOST A2: A1 plus suppression of the end-of-run PTX line report.
# Source audit still must establish whether this flag bypasses collection.
-gpgpu_runtime_stat 500000
-enable_ptx_file_line_stats 0
